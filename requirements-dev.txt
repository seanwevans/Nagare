-r requirements.txt

# Test tooling. `make test` and a clean checkout both depend on this file.
pytest>=8.0,<9.0
pytest-cov>=5.0,<8.0

# Lint tooling; see [tool.ruff] in pyproject.toml.
ruff>=0.16.5,<0.17
